[package]
name = "xwin"
version = "0.2.0"
edition = "2024"

[dependencies]
xela_channels = {
    git = "https://github.com/XelaSpirit/xela_channels.git",
    branch = "master"
}
xela_glfw_sys = {
    git = "https://github.com/XelaSpirit/bindings.git",
    branch = "master",
    features = ["vulkan"]
}